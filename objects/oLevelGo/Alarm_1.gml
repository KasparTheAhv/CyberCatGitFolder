if (watchad) // if watching ad 
{
	Obj_UnityAds.alarm[5]=1;
} else { 
	
var _tex_array = texturegroup_get_textures("Default");
for (var i = 0; i < array_length(_tex_array); ++i;)
{
    texture_prefetch(_tex_array[i]);
}
		if (audio_is_playing(MenuMusic))
		{
		audio_stop_sound(MenuMusic);
		}
	with (oTransition) {target=other.whereto;}
	SlideTransition(TRANS_MODE.GOTO,oTransition.target);
	play_sound(snShoot,false);
}